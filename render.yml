services:
  - type: web
    name: joblens-be
    env: node
    buildCommand: cd JobLens-BE && npm install
    startCommand: cd JobLens-BE && node server
    plan: free
  - type: web
    name: cvextractor
    env: python
    buildCommand: cd cvExtractor && pip install -r requirements.txt
    startCommand: python3 cvExtractor/main.py
    plan: free
